import 'package:get/get.dart';
import 'package:ventpost/controller/home_page_controller.dart';

final hmcontroller = Get.put(Controllerhm());

class Pagecontroller extends GetxController {
  dynamic argumentData = Get.arguments;

  

  post_firebase() {
//   Map<String,dynamic> data = {hmcontroller.deviceid.replaceAll(".","_"):[hmcontroller.deviceid.replaceAll(".","_"),'username','id','len  Datsta']};
// print('clicked');

//     documentReference.update(data).then((value) => print("value updated")).onError((error, stackTrace) => print(error));
  }
}
